let strName = "Vinoth";
strName = "v" + strName.substring(1);
console.log(strName);
console.log(strName.toUpperCase()); // using toUpperCase method we can change the String
console.log(strName.toLowerCase()); // using toLowerCase method we can change the String
